function multiplyNumbers(num1, num2, num3) {
  let result = num1 * num2 * num3;
  return result;
}

let product = multiplyNumbers(2, 3, 4);


// the function arguments - 2, 3, 4
// the function body - Lines 2 & 3
// the function declaration - Lines 1-4
// the function invocation - multiplyNumbers(2, 3, 4) (line 6)
// the function name - multiplyNumbers
// the function parameters - num1, num2, num3 (line 1)
// the function return value - determined by multiplying the arguments together.
// the names of all variables in this program - multiplyNumbers, num1, num2,
//     num3, result, product