console.log('5' + 10);

/*
If either operand of an + expression is a string, then JavaScript coerces the
non-string to a string.
Therefore, the above expression concatenates '5' and '10' resulting in '510' 
*/